[package]
name = "glyphra"
version = "0.1.0-beta.1"
description = "Agent-first, feather-light code studio"
authors = ["Glyphra contributors"]
edition = "2021"
license = "MIT"

[lib]
name = "glyphra_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-dialog = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["rt-multi-thread", "macros", "io-util", "process", "sync", "time", "fs"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
thiserror = "2"
ts-rs = { version = "11", features = ["serde-compat"] }
notify = "8"
sysinfo = "0.35"
keyring = { version = "3", features = ["apple-native", "windows-native", "linux-native-sync-persistent"] }
uuid = { version = "1", features = ["v4", "serde"] }
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls"] }
portable-pty = "0.9.0"
grep = "0.4.1"
ignore = "0.4.30"

[target.'cfg(windows)'.dependencies]
win32job = "2.0.3"
windows = { version = "0.61", features = ["Win32_System_Threading", "Win32_Foundation"] }
windows-version = "0.1"

[profile.release]
codegen-units = 1
lto = true
opt-level = "s"
panic = "abort"
strip = true
